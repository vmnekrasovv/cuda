Есть изменения на локале по сравнению с версией на гитхабе.

В проекте используется gulp-responsive, для создания пресетов изображений под разные разрешения, который зависит от модуля ноды sharp.
npm install sharp - вываливает ошибки, не получается установить. В общем тут разбираться нужно с этим респонсивом.

Выпилил респонсив, сделал, как в teletrade.

В package_back и gulp_back версии для респонсив.